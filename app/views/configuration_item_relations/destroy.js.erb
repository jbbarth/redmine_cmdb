var ids = "#configuration_item_relation_form_<%= @relation.id %>"
ids = ids + ",#configuration_item_relation_box_<%= @relation.id %>"
jQuery(ids).fadeOut('fast', function() { jQuery(this).remove() })
